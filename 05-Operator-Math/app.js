const num1 = 100;
const num2 = 50;
let val;

// Simple math operator
val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

// Math Object
val = Math.PI; // nilai pi
val = Math.E;
val = Math.round(3.7); // Pembulatan
val = Math.ceil(3.1); // Pembulatan ke atas
val = Math.floor(3.1); // Pembulatan ke bawah
val = Math.floor(3.1); // Pembulatan ke bawah
val = Math.sqrt(64); // Akar
val = Math.abs(-5); // Nilai absolut
val = Math.pow(10, 5); // Pemangkatan 10 dengan 5
val = Math.min(1, 23, 4, 5, 123, 123, 1);
val = Math.max(1, 23, 4, 5, 123, 123, 1);
val = Math.random();
val = Math.random() * 50 + 1; // dibawah >=50
val = Math.random() * 50; // dibawah >50





console.log(val);